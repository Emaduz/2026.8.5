# Portrait replacement validation

The newly attached portrait is now served through the existing `/api/portrait` privacy proxy and is used by the shared `PortraitComposition` component. Desktop and mobile Home previews show the replacement image inside the existing circular visual treatment with the orbit paths, asteroid motion, spacing, and responsive crop preserved. The Contact profile card remains intentionally logo-based per the prior user instruction and is not treated as a portrait instance.
