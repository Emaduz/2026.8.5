# دليل ربط النطاق في Cloudflare والاستضافة الخارجية لموقع المهندس عماد الدين

يوضح هذا الدليل الخطوات العملية لربط نطاقك (`emadalddine.com`) عبر **Cloudflare** وتوجيهه إلى استضافة خارجية مستقلة بالكامل دون الاعتماد على ميزات مدفوعة داخل Manus.

---

## 1. فلسفة الربط والاستقلالية
- **Cloudflare:** يظل هو مالك النطاق والمسؤول عن إدارة سجلات DNS وحمايته (CDN & SSL).
- **الاستضافة الخارجية:** تستضيف كود الموقع (Frontend & Backend) أو تفصل الواجهة الثابتة (Static Frontend) على خدمة مثل Vercel / Netlify، وتستضيف لوحة التحكم والبيانات على خادم سحابي مستقل عند الحاجة.

---

## 2. خطوات ربط النطاق في Cloudflare خطوة بخطوة

1. **الدخول إلى لوحة تحكم Cloudflare:**
   - افتح [Cloudflare Dashboard](https://dash.cloudflare.com/) من جهازك وسجل الدخول بحسابك.
   - اختر النطاق الخاص بك: `emadalddine.com`.

2. **الوصول إلى إعدادات DNS:**
   - من القائمة الجانبية، اختر **DNS** ثم **Records**.

3. **إضافة سجل التوجيه (A Record أو CNAME):**
   - اضغط على زر **Add record**.
   - أدخل البيانات بناءً على الاستضافة التي تختارها (مثال للتوجيه المباشر):
     - **Type:** `A` (أو `CNAME` حسب إرشادات الاستضافة)
     - **Name:** `@` (يعني النطاق الرئيسي `emadalddine.com`)
     - **Target / IPv4 address:** عنوان الآيب IP الخاص بالاستضافة الخارجية.
     - **Proxy status:** 
       - اجعلها **DNS only** (السحابة رمادية) أثناء مرحلة الاختبار والربط الأولية لتجنب مشاكل شهادة الأمان SSL.
       - بعد التأكد من عمل الموقع، يمكنك تفعيل السحابة البرتقالية (Proxied) للاستفادة من حماية Cloudflare.

4. **إضافة سجل الـ www (اختياري):**
   - أضف سجل آخر:
     - **Type:** `CNAME`
     - **Name:** `www`
     - **Target:** `emadalddine.com`
     - **Proxy status:** DNS only ثم Proxied.

---

## 3. مقارنة خيارات الاستضافة الخارجية المناسبة لك

| خيار الاستضافة | المزايا | العيوب | التوافق مع مشروعك |
|---|---|---|---|
| **Vercel (للواجهة الثابتة فقط)** | مجاني تماماً، سريع، ربط سهل مع GitHub | لا يدعم خادم قاعدة بيانات MySQL الدائم بنفسه | ممتاز لعرض الموقع الثابت والبورتفوليو |
| **Render / Railway (للنسخة الكاملة)** | تدعم Node.js وقاعدة بيانات MySQL معاً | خطط مجانية محدودة أو تتطلب اشتراك رمزي | ممتاز إذا كنت تريد تشغيل لوحة التحكم وقاعدة البيانات معاً |
| **خادم VPS مستقل (مثل DigitalOcean / Hetzner)** | تحكم كامل، استقلالية 100%، رخيص ($4-$5 شهرياً) | يحتاج إلى إعداد تقني أولي | الخيار الأقوى للمدى البعيد لضمان عدم حذف الحسابات |

---

## 4. الملخص والتنفيذ العملي
بما أنك تفضل الاستقلالية الكاملة، فإن **إدارة النطاق في Cloudflare** مع ربطه باستضافة خارجية (مثل Vercel للواجهة أو خادم مستقل للوحة التحكم) يمنحك سيطرة تامة على موقعك دون أي قيود أو تكاليف غير مرغوبة.
